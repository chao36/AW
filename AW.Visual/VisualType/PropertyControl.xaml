<base:BaseControl 
    x:Class="AW.Visual.VisualType.PropertyControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:base="clr-namespace:AW.Visual.Common"
    mc:Ignorable="d" 
    Background="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=FrameworkElement, AncestorLevel=1}, Path=Background}">
    <Grid
        x:Name="Container"
        Background="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=FrameworkElement, AncestorLevel=1}, Path=Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Label
            VerticalAlignment="Center"
            Content="{Binding Tag}"/>
    </Grid>
</base:BaseControl>
